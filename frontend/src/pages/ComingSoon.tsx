export function ComingSoon({ title }: { title: string }) {
  return (
    <div>
      <h1>{title}</h1>
      <p style={{ color: 'var(--text-secondary)' }}>Todavía no implementado.</p>
    </div>
  )
}
